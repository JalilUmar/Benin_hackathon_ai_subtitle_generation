import React from "react";

export default function Title({ subtitle }: any) {
  // createVttFile(subtitle);
  return (
    <label className="block mb-2 text-lg font-semibold">Upload a Video:</label>
  );
}
